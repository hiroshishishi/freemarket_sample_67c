.bodyUser 
  = render 'items/header'
  .user-main
    = render 'my_left'
    .my-right
      .my-right__logout
        = form_with(url: "#", local: true, method: :get, class: "logout-form") do |form|
          .my-right__logout__content
            = link_to "ログアウト", destroy_user_session_path, class:"btn-logout",method: "delete"
  = render 'items/footer'
